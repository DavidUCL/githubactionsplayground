{
  "name": "action-moodle-playground-verify",
  "version": "0.1.0",
  "private": true,
  "description": "Boot-verify a moodle-playground blueprint headlessly and emit a closed-schema verdict",
  "type": "module",
  "scripts": {
    "test": "node --test test/*.test.mjs",
    "capture-fixtures": "node scripts/capture-fixtures.mjs"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "playwright": "1.61.1"
  },
  "license": "MIT"
}
